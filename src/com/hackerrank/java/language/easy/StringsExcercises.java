package com.hackerrank.java.language.easy;

public class StringsExcercises {

    static int x;
    int y;

//    public static void main(String[] args){

/*        String s = "welcometojava";
        int k = 3;
        String smallest = "";
        String largest = "";
        //ArrayList<String> wordList = new ArrayList<>();
        SortedSet<String> thisSet = new TreeSet<>();

        for(int i=0; i<=((s.length())- k); i++){
            //String thisWord = s.substring(i,(i+k));
            thisSet.add(s.substring(i,(i+k)));
        }
       // Collections.sort(wordList);
        smallest = thisSet.first();
        largest = thisSet.last();
        System.out.println("smallest: " + smallest);
        System.out.println("largest: " + largest);
*/


//        Scanner scan = new Scanner(System.in);
//        String s = scan.nextLine();
//        // Write your code here.
//        scan.close();
/*        java.util.SortedMap<String, Integer> aMap = new java.util.TreeSet<>();
    String s = "                        ";

        if (s == null || s.equals("") || s.trim().equals("")){
            System.out.print("0");
        } else if(s.trim().length() > 0 && s.length() <= 400000){
            s = s.trim();
            s = s.replaceAll("\n+", "");
            s = s.replaceAll("\t+", " ");
            s = s.replaceAll("[^a-zA-Z\\s]", " ");
            s = s.replaceAll("\\s+", " ");

            String[] tokens = s.split(" ");
            System.out.println(tokens.length);
            for(String a : tokens){
                System.out.println(a);
            }
        } else if(s.length() <= 0) {
            System.out.println(0);
        } else {
            System.out.println(0);
        }*/
//    }
}
